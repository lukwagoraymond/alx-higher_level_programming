-- Creates Table called first table in the current database
-- first table description (columns):
-- id INT
-- name VARCHAR(256)
CREATE TABLE IF NOT EXISTS `first_table`
(id, INT,
name VARCHAR(256));
